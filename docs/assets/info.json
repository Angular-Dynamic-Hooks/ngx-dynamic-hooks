---
---

{
  "pages": [
    {% for page in site.pages %}
      {
        "title": "{{ page.title }}",
        "url": "{{ page.url }}"
      }{% unless forloop.last %},{% endunless %}
    {% endfor %}
  ]
}